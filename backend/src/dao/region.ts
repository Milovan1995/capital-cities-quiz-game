//there will be an option to choose a region eventually so i'm adding a bit of a randomising 'flavour' currently to make sure i'm including everything

let regionNumber: number = Math.floor(Math.random() * 5) + 1;
export { regionNumber };
